#this is sample config file rename it to config.py and edit it acordingly

usernamein = "{every username that you want}"
passwordin = "{every password that you want}"

DFP = "database.sqlite"

secret_key = "{place your favrite secret key here}"